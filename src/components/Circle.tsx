import styled from 'styled-components';

const StyledCircle = styled.div`
  background: black;
  height: 500px;
  width: 100%;
`;

const Circle = () => {
  return <StyledCircle />;
}

export default Circle;
